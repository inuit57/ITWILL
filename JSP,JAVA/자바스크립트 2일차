=== , !==

: 값과 자료형 다 동일한지 확인한다.

(* js 에서는 == , != 로 검사할 경우에는 값만 동일하다면 동일한 것으로 판정한다.)

예를 들어서 10 == "10" 이 true로 나온다. 


prompt(message , value)
- 리턴 값 : 사용자가 입력한 값.
- message : 입력창 위에 띄워줄 문자열
- value : 입력창에 기본으로 띄워줄 문장


js에서의 true, false 

- false : null , undefined , 0 , "" 
- true : 그 외의 값들. 
