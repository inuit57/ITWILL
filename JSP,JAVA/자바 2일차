자료형에 어떤 것이 있는지 학습
자료형 각각의 크기.

자동 형변환, 강제 형변환 

상수 값 만들기 : final.

---

추가적인 복습

static final. 
: 클래스 변수로 만드는 final 상수. 전역 변수 라고 생각하면 된다.

메소드 안에서 생성한 변수에 static을 쓰는 것은 불가능. 

--

클래스 변수로 static으로 생성한 놈만 자동으로 0으로 초기화 된다. 

메소드 안에 초기화 안하고 그냥 변수 생성하면 에러 띄우니까 조심. 
